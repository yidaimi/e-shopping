<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ecommerce.mapper.UserMapper">

    <!-- 插入用户，返回自增ID -->
    <insert id="insert" parameterType="com.ecommerce.model.entity.User"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_user (username, password, email, role, created_at)
        VALUES (#{username}, #{password}, #{email}, #{role}, #{createdAt})
    </insert>

    <!-- 按用户名查询用户 -->
    <select id="findByUsername" resultType="com.ecommerce.model.entity.User">
        SELECT id, username, password, email, role, created_at
        FROM sys_user
        WHERE username = #{username}
    </select>

    <!-- 按ID查询用户 -->
    <select id="findById" resultType="com.ecommerce.model.entity.User">
        SELECT id, username, password, email, role, created_at
        FROM sys_user
        WHERE id = #{id}
    </select>

</mapper>
