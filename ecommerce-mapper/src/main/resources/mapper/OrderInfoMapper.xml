<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ecommerce.mapper.OrderInfoMapper">

    <!-- 插入订单，返回自增ID -->
    <insert id="insert" parameterType="com.ecommerce.model.entity.OrderInfo"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO order_info (user_id, total_amount, status, created_at)
        VALUES (#{userId}, #{totalAmount}, #{status}, #{createdAt})
    </insert>

    <!-- 按用户ID查询订单列表，按创建时间倒序 -->
    <select id="findByUserId" resultType="com.ecommerce.model.entity.OrderInfo">
        SELECT id, user_id, total_amount, status, created_at
        FROM order_info
        WHERE user_id = #{userId}
        ORDER BY created_at DESC
    </select>

    <!-- 按ID查询订单 -->
    <select id="findById" resultType="com.ecommerce.model.entity.OrderInfo">
        SELECT id, user_id, total_amount, status, created_at
        FROM order_info
        WHERE id = #{id}
    </select>

</mapper>
